
INSTALL WORDPRESS:

Run docker app from your project

INIT from command line:

`docker-compose up` or

`docker-compose up -d` for detached mode.

#####
http://localhost:8181 to view phpmyadmin
username: wordpress
password: wordpress


http://localhost:8000 to begin wp installations

